# Python
__pycache__/
*.pyc
.venv/
*.log
.DS_Store

# 本地/敏感
credentials*.json
*.secret

# ── 客户数据红线（禁止上传）──
# 客户商品→SKU 映射（仅本地）
adapters/sku_map.json
# 客户行业配置（仅本地）
config/industry_food.json
# 通道 webhook 密钥（仅本地，不入库）
data/channels.json
# 设备直连配置（IP/寄存器地址，仅本地，不入库）
data/machines.json

# 评测产物（本地生成，不入库）
tests/output/
